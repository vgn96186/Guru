import { linearTheme as n } from '../../theme/linearTheme';

export const HOME_SECTION_GAP = n.spacing.md;
export const HOME_TILE_HEIGHT = 135;

/** Below this window width, Home stacks lecture + agenda vertically (phones). */
export const HOME_GRID_STACK_BREAKPOINT = 600;
